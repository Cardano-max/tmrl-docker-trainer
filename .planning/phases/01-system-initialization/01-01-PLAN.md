---
phase: 01-system-initialization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/tmnf_config.json
  - utils/validators.py
  - knowledge/prior_knowledge.py
autonomous: true

must_haves:
  truths:
    - "Config validation rejects configs missing required keys (actions, feedbacks, environment.timing.frame_duration_ms)"
    - "Config validation accepts valid TMNF config without pre-populated bins (bins are discovered at runtime)"
    - "Prior knowledge module detects saved bin discovery JSON files on disk and loads them"
    - "Prior knowledge module returns empty/None when no saved results exist"
    - "Frame duration value exists in TMNF config and is 10ms (TMNF tick rate)"
  artifacts:
    - path: "config/tmnf_config.json"
      provides: "TMNF-specific system configuration"
      contains: "frame_duration_ms"
    - path: "utils/validators.py"
      provides: "Config validation that works with TMNF"
      exports: ["ConfigValidator"]
    - path: "knowledge/prior_knowledge.py"
      provides: "Prior knowledge detection and loading from disk"
      exports: ["PriorKnowledgeManager"]
  key_links:
    - from: "utils/validators.py"
      to: "config/tmnf_config.json"
      via: "ConfigValidator.validate_config()"
      pattern: "validate_config"
    - from: "knowledge/prior_knowledge.py"
      to: "tmnf_phase_a_results_*.json"
      via: "glob pattern matching"
      pattern: "glob.*tmnf_phase_a_results"
---

<objective>
Create the TMNF system config, update config validation for TMNF reality, and implement prior knowledge detection from disk.

Purpose: INIT-01 (config validation), INIT-02 (prior knowledge check), INIT-06 (frame duration from config). These are the foundation pieces that the SystemInitializer will compose in Plan 02.

Output: TMNF config file, updated validator, prior knowledge module.
</objective>

<execution_context>
@C:/Users/ateeb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ateeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing code to modify/reference:
@config/system_config_corrected.json
@config/__init__.py
@utils/validators.py
@utils/exceptions.py
@knowledge/knowledge_manager.py
@adapters/tmnf_adapter.py
@test_phase_a_tmnf.py
@intelligence/intelligence_experimentation.py

# Transcripts (reference for requirements):
# Primary: archive/meeting_transcripts/meeting_transcript_24Jan2026.txt
# Spec: archive/meeting_transcripts/Authoratative_law_from_Jan2026_meetings.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TMNF config and update validator</name>
  <files>config/tmnf_config.json, utils/validators.py</files>
  <action>
  **1. Create `config/tmnf_config.json`:**

  Model it on the existing `system_config_corrected.json` but adapted for TMNF reality:

  ```json
  {
    "system_name": "Sutton_Intelligence_System",
    "version": "1.0.0",
    "description": "TMNF + TMInterface 2.x -- Pure Sutton compliance",

    "actions": {
      "gas": {
        "type": "binary",
        "description": "Throttle -- BINARY in TMNF (>0.001 = full gas ON)",
        "range": [0.0, 1.0]
      },
      "brake": {
        "type": "binary",
        "description": "Brake -- BINARY in TMNF (>0.001 = full brake ON)",
        "range": [0.0, 1.0]
      },
      "steering": {
        "type": "analog",
        "description": "Steering -- ANALOG (-1=full left, +1=full right, maps to -65536..+65536)",
        "range": [-1.0, 1.0]
      }
    },

    "feedbacks": {
      "speed": {
        "description": "Vehicle speed from TMInterface SimStateData",
        "unit": "km/h",
        "interval_size": 5.0,
        "expected_range": [0.0, 500.0]
      }
    },

    "environment": {
      "type": "tmnf",
      "adapter": "adapters.tmnf_adapter.TMNFAdapter",
      "protocol": "tcp",
      "host": "127.0.0.1",
      "port": 8476,
      "timing": {
        "frame_duration_ms": 10,
        "description": "TMNF physics tick = 10ms. MUST match environment. Sutton Jan 24: configured not hard-coded."
      }
    },

    "experimentation": {
      "enabled": true,
      "search_precision": 0.001,
      "min_probe_speed_kmh": 10.0,
      "use_rewind": true,
      "description": "Bin discovery via Sutton downward sweep algorithm"
    },

    "prior_knowledge": {
      "check_on_startup": true,
      "results_dir": ".",
      "results_pattern": "tmnf_phase_a_results_*.json",
      "description": "Sutton Jan 24: if prior knowledge exists, skip experimentation"
    }
  }
  ```

  Key differences from TM2020 config:
  - `frame_duration_ms` is 10 (TMNF tick), not 50 (TM2020 TMRL)
  - Actions do NOT have pre-populated `bins` (bins are DISCOVERED, not configured -- Sutton REQ-20)
  - Action types are `binary`/`analog` (TMNF reality), not all `continuous`
  - Environment section references TMNF adapter and TCP port 8476
  - Prior knowledge section references disk-based JSON results files
  - No FalkorDB/database section (Phase 3 concern, not Phase 1)
  - No `disjoint_rules` at top level (gas/brake mutual exclusion is implicit)
  - No `emergency_fallback_bins` (bins must be discovered per Sutton REQ-20)

  **2. Update `utils/validators.py` ConfigValidator:**

  The existing `ConfigValidator._validate_actions()` requires `bins` key in every action. This is WRONG for TMNF -- bins are discovered at runtime by Phase A. The validator must accept configs where actions have `range` but no `bins`.

  Changes to `ConfigValidator`:

  **DO NOT TOUCH** these (leave exactly as-is): `_validate_feedbacks()`, `InputValidator`, `is_numeric()`.

  **Modify `validate_config()`:**
  - Change `required_top_level` from `['system_name', 'actions', 'feedbacks', 'system_config']` to `['system_name', 'actions', 'feedbacks']`. Remove `system_config` — it is a TM2020/FalkorDB concern (Phase 3), not required for TMNF.
  - Add `'environment'` to `required_top_level` (needed for timing validation).
  - Make `_validate_system_config()` call CONDITIONAL: only call it if `'system_config'` key is present in config. This preserves backward compatibility for TM2020 configs that have it.
  - Add `_validate_environment(config['environment'])` call after feedbacks validation.

  **Modify `_validate_actions()`:**
  - Require `range` (list of 2 numbers) and `type` (string) for ALL action types.
  - Do NOT require `bins`. If `bins` is present, validate it, but it is optional.
  - The existing `type == 'continuous'` branch requires bins — change it so bins are optional for ALL types. If `bins` is present on any type, validate structure (min/max/label). If absent, that's fine (bins are discovered at runtime).

  **Add `_validate_environment(environment)`:**
  - Require `environment` has a `timing` dict with `frame_duration_ms` key.
  - `frame_duration_ms` must be a positive number. This enforces INIT-06.

  **Add `validate_tmnf_config(config)`:**
  - Calls base `validate_config()` first, then TMNF-specific checks (binary/analog type validation, port range, etc.).

  Keep backward compatibility: TM2020 config with `bins` and `system_config` still passes.
  </action>
  <verify>
  Run `python -c "from utils.validators import ConfigValidator; import json; c = json.load(open('config/tmnf_config.json')); ConfigValidator.validate_config(c); print('TMNF config valid')"` -- must print "TMNF config valid".

  Run `python -c "from utils.validators import ConfigValidator; import json; c = json.load(open('config/system_config_corrected.json')); ConfigValidator.validate_config(c); print('TM2020 config still valid')"` -- must print "TM2020 config still valid" (backward compatibility).

  Run `python -c "from utils.validators import ConfigValidator; ConfigValidator.validate_config({'system_name': 'x', 'actions': {}, 'feedbacks': {'s': {'description': 'd', 'unit': 'u', 'interval_size': 1, 'expected_range': [0, 1]}}, 'environment': {'timing': {'frame_duration_ms': 10}}})"` -- must raise ValidationError for empty actions (system_config NOT required since it was removed from required_top_level).

  Run `python -c "from utils.validators import ConfigValidator; ConfigValidator.validate_config({'system_name': 'x', 'actions': {'gas': {'type': 'binary', 'range': [0, 1]}}, 'feedbacks': {'s': {'description': 'd', 'unit': 'u', 'interval_size': 1, 'expected_range': [0, 1]}}, 'environment': {'timing': {'frame_duration_ms': 10}}})"` -- must pass (minimal TMNF config without system_config or bins).
  </verify>
  <done>
  1. `config/tmnf_config.json` exists with TMNF-specific values (frame_duration_ms=10, binary gas/brake, analog steering, no pre-populated bins)
  2. ConfigValidator accepts TMNF config without `bins` in actions
  3. ConfigValidator rejects config missing `environment.timing.frame_duration_ms`
  4. ConfigValidator still accepts old TM2020 config (backward compatible)
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement prior knowledge detection from disk</name>
  <files>knowledge/prior_knowledge.py</files>
  <action>
  Create `knowledge/prior_knowledge.py` with a `PriorKnowledgeManager` class.

  **Sutton context (Jan 24):**
  "when there is previous knowledge... no need to validate anything because the previous knowledge knows everything"
  "check if there is any graph with information... if there is, system starts from there"

  For Phase 1, prior knowledge = saved bin discovery results on disk (JSON files from `test_phase_a_tmnf.py`). In later phases, this will also check FalkorDB graphs, but for now: disk-based JSON files.

  **Class: PriorKnowledgeManager**

  ```python
  class PriorKnowledgeManager:
      """
      Detects and loads prior knowledge from disk.

      Sutton Jan 24: "when there is previous knowledge... no need to validate
      anything because the previous knowledge knows everything"

      Phase 1: Prior knowledge = saved bin discovery JSON files.
      Phase 3+: Will also check FalkorDB graphs.
      """

      def __init__(self, results_dir: str = ".", results_pattern: str = "tmnf_phase_a_results_*.json"):
          self.results_dir = results_dir
          self.results_pattern = results_pattern

      def has_prior_knowledge(self) -> bool:
          """Check if any prior bin discovery results exist on disk."""
          # Use glob to find matching JSON files
          # Return True if at least one valid result file exists

      def load_latest(self) -> Optional[Dict]:
          """Load the most recent bin discovery results.
          Returns parsed JSON dict or None if no results found.
          Sorts by filename (which contains timestamp) to get latest."""

      def load_bins_from_results(self, results: Dict) -> Dict[str, Any]:
          """Extract bins dict from a results JSON file.
          Returns dict mapping action_name -> {min, max, bins, bin_details, ...}
          Compatible with what SystemInitializer expects."""

      def get_all_results(self) -> List[Dict]:
          """Return all saved result files, sorted by timestamp (newest first)."""
  ```

  **Implementation details:**
  - Use `glob.glob()` with `os.path.join(results_dir, results_pattern)` to find files
  - Sort by filename descending (timestamp in name ensures chronological order)
  - Parse JSON, validate it has `results` key with action entries
  - Extract bins from the `results` key structure (match `test_phase_a_tmnf.py` output format)
  - Handle corrupt/invalid JSON gracefully (log warning, skip file)
  - Return bin data in the format SystemInitializer expects:
    ```python
    {
        'gas': {'min': ..., 'max': ..., 'bins': N, 'bin_details': [...], 'source': 'prior_knowledge'},
        'brake': {...},
        'steering': {...}
    }
    ```

  **Key: the JSON format from test_phase_a_tmnf.py save_results():**
  ```json
  {
    "results": {
      "gas": {"max": 1.0, "min": 0.001, "bins": 2, "bin_details": [...]},
      "steering": {"max": 1.0, "min": 0.001, "bins": 201, "bin_details": [...]}
    }
  }
  ```

  Also add `save_results(results: Dict, metadata: Dict = None) -> str` method that saves bin discovery results to disk in the standard format (so the initializer can save after a fresh discovery). This replaces the ad-hoc `save_results()` function in `test_phase_a_tmnf.py`.
  </action>
  <verify>
  Run `python -c "from knowledge.prior_knowledge import PriorKnowledgeManager; pm = PriorKnowledgeManager(); print('has_prior:', pm.has_prior_knowledge()); r = pm.load_latest(); print('latest:', type(r))"` -- must work without errors. If tmnf_phase_a_results_*.json files exist in the current dir, has_prior should be True.

  Run `python -c "
from knowledge.prior_knowledge import PriorKnowledgeManager
import json, tempfile, os
# Create a fake results file
d = tempfile.mkdtemp()
data = {'results': {'gas': {'max': 1.0, 'min': 0.001, 'bins': 2, 'bin_details': [{'id': 0, 'min': 0.0, 'max': 0.001, 'label': 'DEAD_ZONE'}, {'id': 1, 'min': 0.001, 'max': 1.0, 'label': 'ON'}]}}}
with open(os.path.join(d, 'tmnf_phase_a_results_20260225_999999.json'), 'w') as f:
    json.dump(data, f)
pm = PriorKnowledgeManager(results_dir=d)
assert pm.has_prior_knowledge(), 'Should find results'
r = pm.load_latest()
assert r is not None, 'Should load results'
bins = pm.load_bins_from_results(r)
assert 'gas' in bins, 'Should have gas bins'
print('All prior knowledge tests passed')
"` -- must print "All prior knowledge tests passed".
  </verify>
  <done>
  1. `knowledge/prior_knowledge.py` exists with PriorKnowledgeManager class
  2. `has_prior_knowledge()` correctly detects saved JSON result files
  3. `load_latest()` returns the most recent result file parsed as dict
  4. `load_bins_from_results()` extracts bins in the format SystemInitializer expects
  5. Module handles missing files, corrupt JSON, and empty directories gracefully
  </done>
</task>

</tasks>

<verification>
1. TMNF config file loads and passes validation: `python -c "from config import load_config; c = load_config('tmnf_config.json'); print(c['environment']['timing']['frame_duration_ms'])"`
2. Old TM2020 config still passes validation (backward compat)
3. Config with missing frame_duration_ms is rejected
4. Config with actions but no bins passes (TMNF reality)
5. Prior knowledge detection works with existing result files
6. Prior knowledge returns None when no files exist
</verification>

<success_criteria>
- INIT-01 partially met: Config validation catches missing required fields, rejects bad configs
- INIT-02 partially met: Prior knowledge check detects saved bin results on disk
- INIT-06 met: frame_duration_ms=10 in TMNF config, validator ensures it exists
- All existing tests/imports still work (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/01-system-initialization/01-01-SUMMARY.md`
</output>
